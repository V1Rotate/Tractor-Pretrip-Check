import { createApp } from 'vue';
import App from './App.vue';
import './index.css'; //must be imported here to make Tailwind CSS work.

createApp(App).mount('#app'); //createApp one line used in Vue v3.**.
